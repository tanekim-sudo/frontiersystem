def compute_displacement_score(vertical: str, signals: dict) -> float:
    return 0.0


def check_displacement_confirmation(vertical: str, window_days: int = 90) -> dict:
    return {"vertical": vertical, "window_days": window_days, "confirmed": False}
